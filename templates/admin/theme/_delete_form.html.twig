<form method="post" action="{{ path('theme_delete', {'id': theme.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>
