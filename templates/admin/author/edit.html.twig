{% extends 'admin/baseAdmin.html.twig' %}
{% block stylesheets %}
	{{ parent()}}
	<link rel="stylesheet" href="{{ asset('styles/admin/author/index.css')}}">
{% endblock %}
{% block RightPage %}
    <a href="{{ path('author_show', {id:author.id}) }}" class="btn btn-secondary">Retour</a>
    <h1>Edition de l'auteur {{author.name}}</h1>

    {{ include('admin/author/_form.html.twig', {'button_label': 'Modifier'}) }}


    {{ include('admin/author/_delete_form.html.twig') }}
{% endblock %}
