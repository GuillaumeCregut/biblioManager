<form method="post" action="{{ path('author_delete', {'id': author.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>
